@model IEnumerable<ITSB.AccidentDashboard.Core.Entities.InjuryType>
@{ ViewBag.Title = "Injury Types"; }

<div class="container-fluid" style="padding:20px;">
    <div class="page-title-bar">
        <h2>Injury Types</h2>
        @Html.ActionLink("+ Create New", "Create", null, new { @class = "btn btn-primary" })
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">@TempData["Success"]</div>
    }

    <div class="content-card">
        <table class="table">
            <tr><th>Name</th><th style="width:180px;"></th></tr>
            @foreach (var t in Model)
            {
                <tr>
                    <td>@t.Name</td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = t.Id }, new { @class = "btn btn-sm btn-default" })
                        @Html.ActionLink("Delete", "Delete", new { id = t.Id }, new { @class = "btn btn-sm btn-danger" })
                    </td>
                </tr>
            }
        </table>
    </div>
</div>